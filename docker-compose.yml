version: '3.9'

services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - './app.env:/app.env:ro'
    ports:
      - '8080:8080'
    depends_on:
      - mysql
      - redis

  mysql:
    image: mysql:8.0
    restart: always
    environment:
        MYSQL_USER: rapidEx
        MYSQL_DATABASE: rapidEx
        MYSQL_PASSWORD: gvS2yO9C5ZJCwxfK7W
    ports:
      - "3306:3306"
    command: mysqld --character-set-server=utf8 --collation-server=utf8_unicode_ci
    container_name: mysqlDB

  redis:
    image: redis:7.2.3
    restart: always
    command: redis-cli
    ports:
      - "6379:6379"
    container_name: redisDB